{
  "scripts": {
    "build:debug": "docker build -t invertase/react-native-firebase-android:debug docker/debug/",
    "build:release": "docker build -t invertase/react-native-firebase-android:release docker/release/",
    "publish:debug": "docker push invertase/react-native-firebase-android:debug",
    "publish:release": "docker push invertase/react-native-firebase-android:release"
  }
}
